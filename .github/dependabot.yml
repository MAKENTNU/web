---
version: 2
updates:
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
    target-branch: "main"
    commit-message:
      prefix: "⬆"
      include: "scope"

  - package-ecosystem: "github-actions"
    directories:
      # Workflow files stored in the default location of `.github/workflows`
      - "/"
      # Other manifest files, like `action.yml` files in `.github/actions/<action name>`
      - "/.github/actions/**"
    schedule:
      interval: "weekly"
    target-branch: "main"
    commit-message:
      prefix: "👷"
      include: "scope"
